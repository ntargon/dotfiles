{
    "snippets" : [
        {
            "name" : "example",
            "code" : [
                "# This is an example snippet!",
                "# To create your own, add a new snippet block to the",
                "# snippets.json file in your jupyter nbextensions directory:",
                "# /nbextensions/snippets/snippets.json",
                "import this"
            ]
        },
        {
            "name" : "pyplot",
            "code" : [
                "import matplotlib.pyplot as plt",
                "import numpy as np",
                " ",
                "fig, ax = plt.subplots(1, 1, figsize=(8,6))",
                "ax.plot(x, y, label='label')",
                "#ax.errorbar(x, y, yerr=None, xerr=None, fmt='')",
                "#ax.hist(x, bins=10, range=(x.min(), x.max()))",
                "#ax.scatter(bias_list, eff_phits, label='label')",
                "ax.set_xlabel('x_axis')",
                "ax.set_ylabel('y_axis')",
                "ax.set_title('title')",
                "ax.legend(loc='upper right')",
                "#ax.set_xscale('log')",
                "#ax.set_yscale('log')",
                "#ax.set_xlim(1, 1000)",
                "#ax.set_ylim(1, 1000)",
                " ",
                "#ax.tick_params(top=True, right=True, direction='in', which='both')",
                "#ax.tick_params(length=10, which='major')",
                "#ax.tick_params(length=5, which='minor')",
                "#ax.tick_params(labelsize=12)",
                " ",
                "#plt.grid(True)",
                " ",
                "plt.rcParams['font.size'] = 12 #fontsize",
                " "

            ]

        }
    ]
}
